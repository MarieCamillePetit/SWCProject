{% extends 'base.html.twig' %}

{% block title %}ClassMonster index{% endblock %}

{% block body %}
    <h1>ClassMonster index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Class</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for class_monster in class_monsters %}
            <tr>
                <td>{{ class_monster.id }}</td>
                <td>{{ class_monster.class }}</td>
                <td>
                    <a href="{{ path('app_class_monster_show', {'id': class_monster.id}) }}">show</a>
                    <a href="{{ path('app_class_monster_edit', {'id': class_monster.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_class_monster_new') }}">Create new</a>
{% endblock %}
